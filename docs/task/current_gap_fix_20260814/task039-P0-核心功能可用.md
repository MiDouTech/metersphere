# task039 - P0 - 核心功能可用

## 任务

- [x] 个人 Token 编辑前端改为 PATCH，并增加契约检查。
- [x] 六类资产生成可执行、脱敏、不可变快照。
- [x] 环境使用可搜索选择器并在后端校验项目归属。
- [x] 工作台支持最多 50 个资产的添加、移除和固定版本。
- [x] 证据资产可以作为参考资产加入任务。
- [x] 任一资产无效时任务创建整体失败并回滚。

## 验收

- Token 编辑请求不返回 405。
- 任务能同时引用环境、数据集、公共步骤和 API。
- 原资产修改后历史任务上下文不变。
- 上下文含可执行内容或固定内容引用，不只包含名称和摘要。
