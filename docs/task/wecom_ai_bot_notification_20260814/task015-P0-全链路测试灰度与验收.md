# task015 - P0 全链路测试、灰度与验收

> 状态：本地自动化门禁通过，待真实企微全链路、故障演练与灰度验收  
> 前置依赖：task008、task009、task010、task012、task013、task014  
> 阻塞任务：发布  
> 关联方案：§14、§15、§17

## 1. 任务目标

完成自动化测试、真实企业微信测试 Bot 验收、故障演练、灰度观察和发布门禁，禁止以单次手工发送成功代替全链路验收。

## 2. 自动化门禁

本轮已完成的本地门禁（2026-08-14）：

- [x] Bridge 8 项单测通过，覆盖重启后幂等持久化及回调失败不反转发送结果；
- [x] Java `system-setting` 依赖联编通过；新增 3 项 WeCom 单测并通过；
- [x] 前端 `vue-tsc --noEmit --skipLibCheck` 通过；
- [x] Flyway `3.7.2` 文件版本扫描无重复版本号；
- [ ] 下列数据库、真实网络、多实例和浏览器交互门禁仍需在部署环境执行。

- [ ] Bridge 单测：认证状态机、心跳、重连、幂等、错误映射；
- [ ] Java 单测：规则校验、对象解析、模板、Timer、Outbox；
- [ ] Mapper/Flyway 测试；
- [ ] Java ↔ mock Bridge 集成测试；
- [ ] MySQL + Redis + Quartz 多实例并发测试；
- [ ] 前端类型检查、单测和关键交互测试；
- [ ] 权限矩阵和内部回调安全测试；
- [ ] 现有通知、缺陷、测试报告回归。

## 3. 真实企微验收

### 3.1 连接与目标

- [ ] Bot 认证成功并稳定心跳；
- [ ] 测试 userid 个人消息成功；
- [ ] 不可见用户返回明确错误；
- [ ] 群内 @Bot 后发现 chatid；
- [ ] 群主动消息成功；
- [ ] Bot 移出群后错误可诊断。

### 3.2 缺陷提醒

- [ ] 提前量和间隔准确；
- [ ] 处理人/创建人去重；
- [ ] 修改时间/处理人后重算；
- [ ] 关闭、删除、清空时间后停止；
- [ ] 到期后停止；
- [ ] 无 userid 用户仅影响自身目标。

### 3.3 报告通知

- [ ] 手工和自动生成均触发；
- [ ] 事务回滚不触发；
- [ ] 每个配置群一条；
- [ ] 默认 @ 项目有效成员；
- [ ] 指定人员规则生效；
- [ ] 大成员量和非群成员降级正确。

### 3.4 通用通知

- [ ] Cron + 时区准确；
- [ ] 个人/群/BOTH 正确；
- [ ] 启停、有效期、立即执行正确；
- [ ] misfire 不集中补发。

## 4. 故障演练

- Bridge 重启；
- Java 重启；
- Redis 短暂不可用；
- MySQL 短暂不可用；
- WSS 网络中断与恢复；
- Secret 错误与轮换；
- 企业微信限流；
- 发送后 Java 落状态前崩溃；
- 多实例竞争 Timer/Outbox。

## 5. 灰度步骤

1. 后端和表结构先上线，功能默认关闭；
2. Bridge 上线但不启用 Bot；
3. 仅管理员和两个测试用户可见；
4. 启用一个测试群；
5. 先人工测试，再启用低频 Cron；
6. 启用一个测试项目的缺陷/报告规则；
7. 连续观察 24～48 小时；
8. 无阻断问题后逐步扩展项目和成员。

## 6. 发布门禁

- [ ] 无 P0/P1 缺陷；
- [ ] Secret 安全检查通过；
- [ ] 无重复发送或消息丢失；
- [ ] 关键告警可触发；
- [ ] 回滚和紧急停用演练通过；
- [ ] 产品、测试、研发、运维共同签字确认；
- [ ] task000 里程碑状态同步更新。
