# task036 - P1 - 六类测试资产接入

- 接入 DATASET、ENVIRONMENT、COMMON_STEP、API_DEFINITION、EVIDENCE、BUG。
- 原业务表是唯一事实来源，资产中心不得复制业务 CRUD。
- 单类必须同时具备目录页面、聚合 API、项目隔离、权限、不可变版本、关系、Agent 脱敏上下文和测试。
- 任务创建支持显式 `assetRefs` 并固定版本；后续资产修改不得改变历史任务上下文。
- 六类均完成后统一开放页面，禁止空 Tab。
