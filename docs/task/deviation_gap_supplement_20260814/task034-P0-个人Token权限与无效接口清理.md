# task034 - P0 - 个人 Token 权限与无效接口清理

- 查询使用个人 Agent READ；创建、更新、启停使用 CONNECT；删除使用 REVOKE。
- 前端入口、按钮和后端接口使用同一权限。
- 服务层保留本人归属校验并补跨用户拒绝测试。
- 删除 `GET /delete/{id}` 破坏性兼容接口。
- 删除仅返回记录、没有真实验证行为的 `/{id}/test` 及前端封装。
